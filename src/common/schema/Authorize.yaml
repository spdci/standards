type: object
description: |
  @context: "https://standards.spdci.org/schemas//Authorize" <br>
  @type: "Authorize"
example:
  "@context": "https://standards.spdci.org/schemas//Authorize"
  "@type": "Authorize"
  ts:
    $ref: ./DateTime.yaml
  purpose:
    text:
      type: string
    code: 
      type: string
      description: From a fixed set, documented at refUri
    refUri:
        type: string
        format: uri
        description: Uri to provide more info on authorize codes

