type: object
description: |
  @context: "https://spdci.github.io/schemas//Consent" <br>
  @type: "Consent"
example:
  "@context": "https://spdci.github.io/schemas//Consent"
  "@type": "Consent"
  ts:
    $ref: ./DateTime.yaml
  purpose:
    text:
      type: string
    code: 
      type: string
      description: From a fixed set, documented at refUri
    refUri:
        type: string
        format: uri
        description: Uri to provide more info on consent codes